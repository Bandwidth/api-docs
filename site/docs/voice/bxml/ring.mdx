---
id: ring
title: Ring
slug: /voice/bxml/ring
description: A general overview of Bandwidth's Ring BXML Verb
keywords:
  - bandwidth
  - voice
  - bxml
  - ring
hide_title: false
image: ../../static/img/bandwidth-logo.png
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

The Ring verb is used to play ringing audio on a call.

## Text Content

There is no text content available to be set for the `<Ring>` verb.

## Attributes

| ATTRIBUTE | Description                                                                                            |
|:----------|:-------------------------------------------------------------------------------------------------------|
| duration  | (optional) How many seconds to play ringing on the call. Default value is 5. Range: decimal values between 0.1 - 86400. |

## Webhooks Received

NThere are no webhooks received after the `<Ring>` verb is executed.

## Examples

### Play Ringing to a Call

<Tabs
  groupId="bxml"
  defaultValue="xml"
  values={[
    { label: 'XML', value: 'xml', },
    { label: 'Java', value: 'java', },
    { label: 'C#', value: 'csharp', },
    { label: 'Ruby', value: 'ruby', },
    { label: 'NodeJS', value: 'nodejs', },
    { label: 'Python', value: 'python', },
    { label: 'PHP', value: 'php', },
  ]
}>
<TabItem value="xml">

```xml
<?xml version="1.0" encoding="UTF-8"?>
<Response>
   <Ring duration="10" />
</Response>
```

</TabItem>
<TabItem value="java">

```java
Ring ring = Ring.builder()
        .duration(10.0)
        .answerCall(false)
        .build();

Response response = Response.builder().build()
        .add(ring);

System.out.println(response.toBXML());
```

</TabItem>
<TabItem value="csharp">

```cs
Ring ring = new Ring
{
    Duration = 10,
    AnswerCall = false
};

var response = new Response();
response.Add(ring);

Console.WriteLine(response.ToBXML());
```

</TabItem>
<TabItem value="ruby">

```ruby
response = Bandwidth::Voice::Response.new()
ring = Bandwidth::Voice::Ring.new({
    :duration => 10,
    :answer_call => false
})

response.push(ring)
puts response.to_bxml()
```

</TabItem>
<TabItem value="nodejs">

```js
const ring = new Ring({
    duration: 10,
    answerCall: false
});

const response = new Response(ring);

console.log(response.toBxml());
```

</TabItem>
<TabItem value="python">

```python
ring = Ring(
    duration=10,
    answer_call=False
)

response = Response()
response.add_verb(ring)

print(response.to_bxml())
```

</TabItem>
<TabItem value="php">

```php
$ring = new BandwidthLib\Voice\Bxml\Ring();
$ring->duration(10);
$ring->answerCall(false);

$response = new BandwidthLib\Voice\Bxml\Response();
$response->addVerb($ring);

echo $response->toBxml();
```

</TabItem>
</Tabs>
