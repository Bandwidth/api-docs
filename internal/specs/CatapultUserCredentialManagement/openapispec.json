{
  "swagger": "2.0",
  "info": {
    "description": "These endpoints allow a user with the SERVICE role to manage catapult api credentials on behalf of a user. They are intended for use by Iris.",
    "version": "2018.13",
    "title": "CatapultUserCredentialManagement"
  },
  "host": "api.stage.catapult.inetwork.com",
  "basePath": "/v1",
  "schemes": [
    "https"
  ],
  "paths": {
    "/users/{userId}/apiTokens": {
      "post": {
        "tags": [
          "create"
        ],
        "summary": "Create API token for user",
        "operationId": "create",
        "produces": [
          "HTTP Status"
        ],
        "parameters": [
          {
            "name": "userId",
            "in": "path",
            "description": "the ID of the user on behalf of whom you are managing tokens",
            "type": "string",
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "schema": {
              "$ref": "#/definitions/ApiToken"
            }
          },
          "403": {
            "description": "Your user does not have the SERVICE role"
          }
        }
      },
      "get": {
        "tags": [
          "list"
        ],
        "summary": "List all API tokens for user",
        "operationId": "list",
        "produces": [
          "application/json"
        ],
        "parameters": [
          {
            "name": "userId",
            "in": "path",
            "description": "the ID of the user on behalf of whom you are managing tokens",
            "type": "string",
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "successful operation",
            "schema": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ApiToken"
              }
            }
          },
          "403": {
            "description": "Your user does not have the SERVICE role"
          }
        }
      }
    },
    "/users/{userId}/apiTokens/{tokenId}": {
      "delete": {
        "tags": [
          "delete"
        ],
        "summary": "Delete API token for user",
        "operationId": "delete",
        "produces": [
          "HTTP Status"
        ],
        "parameters": [
          {
            "name": "userId",
            "in": "path",
            "description": "the ID of the user on behalf of whom you are managing tokens",
            "type": "string",
            "required": true
          },
          {
            "name": "tokenId",
            "in": "path",
            "description": "the ID of the token to delete",
            "type": "string",
            "required": true
          }
        ],
        "responses": {
          "204": {
            "description": "No content—successfully deleted"
          },
          "403": {
            "description": "Your user does not have the SERVICE role"
          },
          "404": {
            "description": "The specified token does not exist, or does not belong to the specified user"
          }
        }
      }
    }
  },
  "definitions": {
    "ApiToken": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "the ID of this API token (used as the \"username\" in basic auth)",
          "example": "t-646vdsrpuecb6oijbbry4pq"
        },
        "secret": {
          "type": "string",
          "description": "the secret portion of this API token (used as \"password\" in basic auth)",
          "example": "tegkczjjyqcs7i4pmikr4nk4qb3jjxa3hiurc6y"
        }
      }
    }
  }
}
