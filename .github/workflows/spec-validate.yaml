# Validates the spec in this repo

name: OAS Validate

on:
  pull_request:
    branches:
      - 'main'
    paths:
      - 'specs/**'

jobs:
  validate:
    name: Spec validate
    runs-on: ubuntu-latest
    steps:
      - name: Repo Checkout
        uses: actions/checkout@v2
      - name: Devx Utils Checkout
        uses: actions/checkout@v2
        with:
          repository: 'Bandwidth/devx-utils-actions'
          path: 'devx-utils'
          token: ${{ secrets.DX_GIT_TOKEN }}
      - name: Validate spec
        uses: ./devx-utils/validate
        with:
          specPath: './specs'
        env:
          BW_GITHUB_USERNAME: ${{ secrets.DX_GIT_USER }}
          BW_GITHUB_PASSWORD: ${{ secrets.DX_GIT_TOKEN }} 
          APIMATIC_ACCESS_TOKEN: ${{ secrets.APIMATIC_ACCESS_TOKEN }}
